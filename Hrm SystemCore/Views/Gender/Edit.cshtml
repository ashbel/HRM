@model Hrm_SystemCore.Models.tblGender

@{
    ViewBag.Title = "Gender";
}

<h2>Gender</h2>

@using (Html.BeginForm()) {
    @Html.AntiForgeryToken()
    @Html.ValidationSummary(true)

    <fieldset>
        <legend></legend>
        @Html.HiddenFor(model=>model.emp_sx_id)
        <div class="editor-label">
           Gender
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.emp_sex)
            @Html.ValidationMessageFor(model => model.emp_sex)
        </div>

        <p>
            <input type="submit" value="Save" />
        </p>
    </fieldset>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>

@section Scripts {
    @*@Scripts.Render("~/bundles/jqueryval")*@
}
