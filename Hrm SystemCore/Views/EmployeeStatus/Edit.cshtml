@model Hrm_SystemCore.Models.tblEmployeeStatu

@{
    ViewBag.Title = "Employee Status";
}

<h2>Edit - Employee Status</h2>

@using (Html.BeginForm()) {
    @Html.AntiForgeryToken()
    @Html.ValidationSummary(true)

    <fieldset>
        <legend></legend>
        @Html.HiddenFor(model => model.empst_id)
        <div class="editor-label">
            Status
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.empst_title)
            @Html.ValidationMessageFor(model => model.empst_title)
        </div>

        <div class="editor-label">
            Description
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.empst_descr)
            @Html.ValidationMessageFor(model => model.empst_descr)
        </div>

        <p>
            <input type="submit" value="Save" />
        </p>
    </fieldset>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>

@section Scripts {
    @*@Scripts.Render("~/bundles/jqueryval")*@
}
