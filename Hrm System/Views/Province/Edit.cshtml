@model Hrm_System.Models.tblProvince

@{
    ViewBag.Title = "Edit";
}

<h2>Edit</h2>

@using (Html.BeginForm()) {
    @Html.AntiForgeryToken()
    @Html.ValidationSummary(true)

    <fieldset>
        <legend>tblProvince</legend>
        <div class="editor-label">
            @Html.LabelFor(model => model.prov_name)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.prov_name)
            @Html.ValidationMessageFor(model => model.prov_name)
        </div>

        <p>
            <input type="submit" value="Save" />
        </p>
    </fieldset>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
